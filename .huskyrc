{
  "hooks": {
    "pre-commit": "git stash push -ku && npm run lint && npm test && (git stash pop || true)",
    "pre-push": "git stash push -u && npm run lint && npm test && (git stash pop || true)"
  }
}
